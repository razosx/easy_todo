// Smoke test placeholder — la app requiere Firebase inicializado para correr.
// Los tests de integración end-to-end se implementan en la Fase 8.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — app tests are in feature-specific test files', () {
    expect(true, isTrue);
  });
}
